export async function getUSDAData(commodity, region, size) {
  // TODO: Connect to backend API /api/usda/commodity
  return { data: "Stubbed USDA API result" };
}
export function normalizeUSDAResponse(data) {
  return data;
}
