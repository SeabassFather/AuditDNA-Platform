      {/* COMPARISON DATA DISPLAY WITH TIMEZONE PRICING */}
      {comparisonMode && Object.keys(commodityData).length > 0 && (
        <>
          {/* COMPARISON CARDS */}
          <div style={{
            background: 'rgba(255, 255, 255, 0.7)',
            backdropFilter: 'blur(12px)',
            borderRadius: '16px',
            padding: '2rem',
            marginBottom: '2rem',
            boxShadow: '0 4px 12px rgba(0, 0, 0, 0.08)',
            border: '1px solid rgba(34, 197, 94, 0.2)'
          }}>
            <h2 style={{ fontSize: '1.5rem', fontWeight: 'bold', color: '#166534', marginBottom: '2rem' }}>
              🔍 Comparison Results
            </h2>
            
            <div style={{
              display: 'grid',
              gridTemplateColumns: 'repeat(auto-fit, minmax(320px, 1fr))',
              gap: '1.5rem'
            }}>
              {selectedCommodities.map(commodity => {
                const data = commodityData[commodity.id];
                if (!data) return null;
                
                return (
                  <div
                    key={commodity.id}
                    style={{
                      background: 'rgba(255, 255, 255, 0.9)',
                      border: '2px solid rgba(34, 197, 94, 0.3)',
                      borderRadius: '12px',
                      padding: '1.5rem',
                      boxShadow: '0 4px 12px rgba(0, 0, 0, 0.06)'
                    }}
                  >
                    <div style={{ display: 'flex', alignItems: 'center', gap: '1rem', marginBottom: '1rem' }}>
                      <span style={{ fontSize: '2.5rem' }}>{commodity.icon}</span>
                      <div style={{ flex: 1 }}>
                        <h3 style={{ fontSize: '1.2rem', fontWeight: 'bold', color: '#166534', margin: 0 }}>
                          {commodity.name}
                        </h3>
                        <p style={{ fontSize: '0.85rem', color: '#71717a', margin: '0.25rem 0 0 0' }}>
                          {data.variety} • {data.region}
                        </p>
                      </div>
                    </div>
                    
                    <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
                      <div style={{
                        display: 'flex',
                        justifyContent: 'space-between',
                        padding: '0.75rem',
                        background: 'rgba(34, 197, 94, 0.08)',
                        borderRadius: '8px'
                      }}>
                        <span style={{ color: '#71717a', fontSize: '0.85rem', fontWeight: '600' }}>FOB Price:</span>
                        <span style={{ fontWeight: 'bold', color: '#22c55e', fontSize: '1.1rem' }}>
                          ${data.wholesalePrice?.toFixed(2)}
                        </span>
                      </div>
                      
                      <div style={{
                        display: 'flex',
                        justifyContent: 'space-between',
                        padding: '0.75rem',
                        background: 'rgba(59, 130, 246, 0.08)',
                        borderRadius: '8px'
                      }}>
                        <span style={{ color: '#71717a', fontSize: '0.85rem', fontWeight: '600' }}>Margin:</span>
                        <span style={{ fontWeight: 'bold', color: '#3b82f6', fontSize: '1.1rem' }}>
                          {data.marginPercent}%
                        </span>
                      </div>
                      
                      <div style={{
                        display: 'flex',
                        justifyContent: 'space-between',
                        padding: '0.75rem',
                        background: 'rgba(139, 92, 246, 0.08)',
                        borderRadius: '8px'
                      }}>
                        <span style={{ color: '#71717a', fontSize: '0.85rem', fontWeight: '600' }}>Volume:</span>
                        <span style={{ fontWeight: 'bold', color: '#8b5cf6', fontSize: '1.1rem' }}>
                          {data.volume?.toLocaleString()}
                        </span>
                      </div>
                      
                      <div style={{
                        display: 'flex',
                        justifyContent: 'space-between',
                        padding: '0.75rem',
                        background: data.riskIndex > 0.5
                          ? 'rgba(239, 68, 68, 0.08)'
                          : 'rgba(16, 185, 129, 0.08)',
                        borderRadius: '8px'
                      }}>
                        <span style={{ color: '#71717a', fontSize: '0.85rem', fontWeight: '600' }}>Risk Index:</span>
                        <span style={{ 
                          fontWeight: 'bold', 
                          color: data.riskIndex > 0.5 ? '#ef4444' : '#10b981',
                          fontSize: '1.1rem'
                        }}>
                          {(data.riskIndex * 100).toFixed(0)}/100
                        </span>
                      </div>
                    </div>
                  </div>
                );
              })}
            </div>
          </div>

          {/* TIMEZONE PRICING BREAKDOWN */}
          {selectedCommodities.length > 0 && commodityData[selectedCommodities[0].id]?.timezonePricing && (
            <div style={{
              background: 'rgba(255, 255, 255, 0.7)',
              backdropFilter: 'blur(12px)',
              borderRadius: '16px',
              padding: '2rem',
              marginBottom: '2rem',
              boxShadow: '0 4px 12px rgba(0, 0, 0, 0.08)',
              border: '1px solid rgba(34, 197, 94, 0.2)'
            }}>
              <h2 style={{ fontSize: '1.5rem', fontWeight: 'bold', color: '#166534', marginBottom: '2rem', display: 'flex', alignItems: 'center', gap: '0.75rem' }}>
                <Globe size={28} style={{ color: '#22c55e' }} />
                Multi-Timezone Price Comparison
              </h2>
              
              <div style={{
                display: 'grid',
                gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
                gap: '1.5rem'
              }}>
                {Object.entries(commodityData[selectedCommodities[0].id].timezonePricing).map(([tz, pricing]) => {
                  const tzOption = timezoneOptions.find(t => t.id === tz);
                  if (!filters.timezone.includes(tz)) return null;
                  
                  return (
                    <div
                      key={tz}
                      style={{
                        background: 'rgba(255, 255, 255, 0.9)',
                        border: `2px solid ${tzOption?.color}40`,
                        borderRadius: '12px',
                        padding: '1.5rem',
                        boxShadow: `0 4px 12px ${tzOption?.color}20`
                      }}
                    >
                      <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', marginBottom: '1rem' }}>
                        <Clock size={24} style={{ color: tzOption?.color }} />
                        <div>
                          <h4 style={{ fontSize: '1.1rem', fontWeight: 'bold', color: '#166534', margin: 0 }}>
                            {tz}
                          </h4>
                          <p style={{ fontSize: '0.75rem', color: '#71717a', margin: '0.25rem 0 0 0' }}>
                            {pricing.location}
                          </p>
                        </div>
                      </div>
                      
                      <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem' }}>
                        <div style={{
                          display: 'flex',
                          justifyContent: 'space-between',
                          padding: '0.5rem',
                          background: `${tzOption?.color}08`,
                          borderRadius: '6px'
                        }}>
                          <span style={{ fontSize: '0.8rem', color: '#71717a' }}>FOB:</span>
                          <span style={{ fontSize: '0.9rem', fontWeight: '600', color: '#3f3f46' }}>
                            ${pricing.fob.toFixed(2)}
                          </span>
                        </div>
                        
                        <div style={{
                          display: 'flex',
                          justifyContent: 'space-between',
                          padding: '0.5rem',
                          background: `${tzOption?.color}08`,
                          borderRadius: '6px'
                        }}>
                          <span style={{ fontSize: '0.8rem', color: '#71717a' }}>Wholesale:</span>
                          <span style={{ fontSize: '0.9rem', fontWeight: '600', color: '#3f3f46' }}>
                            ${pricing.wholesale.toFixed(2)}
                          </span>
                        </div>
                        
                        <div style={{
                          display: 'flex',
                          justifyContent: 'space-between',
                          padding: '0.5rem',
                          background: `${tzOption?.color}08`,
                          borderRadius: '6px'
                        }}>
                          <span style={{ fontSize: '0.8rem', color: '#71717a' }}>Retail:</span>
                          <span style={{ fontSize: '0.9rem', fontWeight: '600', color: '#3f3f46' }}>
                            ${pricing.retail.toFixed(2)}
                          </span>
                        </div>
                        
                        <div style={{
                          display: 'flex',
                          justifyContent: 'space-between',
                          padding: '0.5rem',
                          background: `${tzOption?.color}15`,
                          borderRadius: '6px',
                          border: `1px solid ${tzOption?.color}40`
                        }}>
                          <span style={{ fontSize: '0.8rem', color: '#71717a', fontWeight: '600' }}>Freight:</span>
                          <span style={{ fontSize: '0.9rem', fontWeight: 'bold', color: tzOption?.color }}>
                            ${pricing.freight.toFixed(2)}
                          </span>
                        </div>
                      </div>
                    </div>
                  );
                })}
              </div>
            </div>
          )}

          {/* COST BREAKDOWN */}
          {selectedCommodities.length > 0 && commodityData[selectedCommodities[0].id]?.costBreakdown && (
            <div style={{
              background: 'linear-gradient(135deg, rgba(34, 197, 94, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%)',
              backdropFilter: 'blur(12px)',
              borderRadius: '16px',
              padding: '2rem',
              marginBottom: '2rem',
              boxShadow: '0 4px 12px rgba(0, 0, 0, 0.08)',
              border: '2px solid rgba(34, 197, 94, 0.3)'
            }}>
              <h2 style={{ fontSize: '1.5rem', fontWeight: 'bold', color: '#166534', marginBottom: '2rem', display: 'flex', alignItems: 'center', gap: '0.75rem' }}>
                <DollarSign size={28} style={{ color: '#22c55e' }} />
                Detailed Cost Breakdown
              </h2>
              
              <div style={{
                background: 'rgba(255, 255, 255, 0.9)',
                borderRadius: '12px',
                padding: '2rem',
                maxWidth: '600px'
              }}>
                {Object.entries(commodityData[selectedCommodities[0].id].costBreakdown).map(([key, value]) => {
                  if (key === 'total') {
                    return (
                      <div
                        key={key}
                        style={{
                          display: 'flex',
                          justifyContent: 'space-between',
                          padding: '1rem',
                          marginTop: '0.5rem',
                          background: 'linear-gradient(135deg, #22c55e 0%, #16a34a 100%)',
                          color: '#fff',
                          borderRadius: '8px',
                          fontWeight: 'bold',
                          fontSize: '1.1rem'
                        }}
                      >
                        <span>TOTAL COST:</span>
                        <span>${value.toFixed(2)}</span>
                      </div>
                    );
                  }
                  
                  return (
                    <div
                      key={key}
                      style={{
                        display: 'flex',
                        justifyContent: 'space-between',
                        padding: '0.75rem',
                        marginBottom: '0.5rem',
                        background: 'rgba(34, 197, 94, 0.05)',
                        borderRadius: '6px'
                      }}
                    >
                      <span style={{ color: '#71717a', textTransform: 'capitalize' }}>
                        {key.replace(/([A-Z])/g, ' $1').trim()}:
                      </span>
                      <span style={{ fontWeight: '600', color: '#3f3f46' }}>
                        ${value.toFixed(2)}
                      </span>
                    </div>
                  );
                })}
              </div>
            </div>
          )}

          {/* EXPORT OPTIONS */}
          <div style={{
            background: 'rgba(255, 255, 255, 0.7)',
            backdropFilter: 'blur(12px)',
            borderRadius: '16px',
            padding: '2rem',
            boxShadow: '0 4px 12px rgba(0, 0, 0, 0.08)',
            border: '1px solid rgba(34, 197, 94, 0.2)'
          }}>
            <h4 style={{ fontSize: '1.1rem', fontWeight: '600', color: '#166534', marginBottom: '1rem', display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
              <Share2 size={24} style={{ color: '#22c55e' }} />
              Export & Share Options
            </h4>
            
            <div style={{ display: 'flex', gap: '1rem', flexWrap: 'wrap' }}>
              <button style={{
                padding: '0.75rem 1.5rem',
                background: 'linear-gradient(135deg, #22c55e 0%, #16a34a 100%)',
                color: '#fff',
                border: 'none',
                borderRadius: '8px',
                cursor: 'pointer',
                fontWeight: '600',
                display: 'flex',
                alignItems: 'center',
                gap: '0.5rem',
                boxShadow: '0 4px 12px rgba(34, 197, 94, 0.3)'
              }}>
                <Download size={18} />
                Export to PDF
              </button>
              
              <button style={{
                padding: '0.75rem 1.5rem',
                background: 'linear-gradient(135deg, #3b82f6 0%, #2563eb 100%)',
                color: '#fff',
                border: 'none',
                borderRadius: '8px',
                cursor: 'pointer',
                fontWeight: '600',
                display: 'flex',
                alignItems: 'center',
                gap: '0.5rem',
                boxShadow: '0 4px 12px rgba(59, 130, 246, 0.3)'
              }}>
                <FileText size={18} />
                Export to Excel
              </button>
              
              <button style={{
                padding: '0.75rem 1.5rem',
                background: 'linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%)',
                color: '#fff',
                border: 'none',
                borderRadius: '8px',
                cursor: 'pointer',
                fontWeight: '600',
                display: 'flex',
                alignItems: 'center',
                gap: '0.5rem',
                boxShadow: '0 4px 12px rgba(139, 92, 246, 0.3)'
              }}>
                <Mail size={18} />
                Email Report
              </button>
              
              <button style={{
                padding: '0.75rem 1.5rem',
                background: 'linear-gradient(135deg, #f59e0b 0%, #d97706 100%)',
                color: '#fff',
                border: 'none',
                borderRadius: '8px',
                cursor: 'pointer',
                fontWeight: '600',
                display: 'flex',
                alignItems: 'center',
                gap: '0.5rem',
                boxShadow: '0 4px 12px rgba(245, 158, 11, 0.3)'
              }}>
                <Share2 size={18} />
                Share Link
              </button>
            </div>
          </div>
        </>
      )}